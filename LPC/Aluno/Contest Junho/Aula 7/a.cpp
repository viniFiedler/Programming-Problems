#include <bits/stdc++.h>
#define MAX 100

int J(int calcula[MAX], i, j, int pont)
{
    if (i == j)
    {
        return calcula[i];
    }

    pont = max()
}

int main()
{
    int n;
    int vet[3000][3000];

    cin >> n;

    for (int i = 0; i < n; i++)
    {
        int num;
        cin >> num;
        vet[i] = num;
    }

    

    return 0;
}